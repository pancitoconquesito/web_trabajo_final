import { MinutosParseHrMinPipe } from './minutos-parse-hr-min.pipe';

describe('MinutosParseHrMinPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutosParseHrMinPipe();
    expect(pipe).toBeTruthy();
  });
});
